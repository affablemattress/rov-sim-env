#include "config.hpp"

namespace cfg {
    namespace window {
        const uint16_t width = 1280;
        const uint16_t height = 720;
        const float fov = 75.f;
        const char* title = "rov-sim-env";
    }
}